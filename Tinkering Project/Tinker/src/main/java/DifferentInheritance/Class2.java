//package DifferentInheritance;
//
//public class Class2 extends Class1{
//}

/**
 * An interface cannot be inherited by a non-interface class because if the super is not overridden, then the methods
 * are not defined. Interfaces cannot be instantiated and therefore, in order to not run into a compilation error, they
 * cannot be inherited by a non-interface class. Furthermore, an interface cannot inherit a non-interface class.
 */
